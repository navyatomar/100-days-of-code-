code 5
#include <stdio.h>

int main() {
    float c;
    scanf("%f", &c);

    float f = (c * 9 / 5) + 32;

    printf("Fahrenheit=%.0f", f);

    return 0;
}
code=6
#include <stdio.h>

int main() {
    int a, b, temp;
    scanf("%d %d", &a, &b);

    temp = a;
    a = b;
    b = temp;

    printf("After swap: %d %d", a, b);

    return 0;
}
